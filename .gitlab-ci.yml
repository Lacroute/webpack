cache:
  paths:
    - ./node_modules/

build:
  stage: build
  script:
    - npm install && gulp build --lang fr-FR
    - cp -R ./build /home/gitlab-runner/deploy/
    - rm -rf $PWD
  only:
    - master
  tags:
    - deployBuild
  allow_failure: true
